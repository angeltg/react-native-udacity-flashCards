import React, { component } from "react";
import { View } from "react-native";

class Home extends component {
  render() {
    return (
      <View>
        <Text>La home</Text>
      </View>
    );
  }
}
